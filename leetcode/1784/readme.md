# LeetCode 1784 - Check if Binary String Has at Most One Segment of Consecutive Ones

## Problem
Determine whether a binary string (without leading zeros) contains at most one contiguous segment of ones.

## Solution
Check for the substring `"01"`.

## Complexity
Let n = length of the string.
- Time: O(n)
- Space: O(1)
