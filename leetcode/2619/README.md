# LeetCode 2619 - Array Prototype Last

## Problem
Implement a method on the Array prototype that returns the last element, or -1 if the array is empty.

## Solution
Extend the Array prototype with a `last` method that checks the array length and returns the last element using index access, or -1 for empty arrays.

## Complexity
- Time: O(1)
- Space: O(1)