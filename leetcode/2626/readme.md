# LeetCode 2626 - Array Reduce Transformation

## Problem
Implement a custom reduce function that applies a reducer function to accumulate array elements into a single value.

## Solution
Iterate through the array, applying the reducer function to accumulate each element into the result.

## Complexity
Let n = length of the input array.
- Time: O(n)
- Space: O(1)